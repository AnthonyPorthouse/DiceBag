build:
    environment:
        php:
            version: 7.0

checks:
    php:
        code_rating: true
        duplication: true

tools:
    php_code_sniffer:
        config:
            standard: "PSR2"
